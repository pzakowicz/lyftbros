CREATE TABLE sets (id INTEGER AUTO_INCREMENT PRIMARY KEY, exercise_id INTEGER NOT NULL, weight INTEGER NOT NULL, REPS INTEGER NOT NULL, workout_id INTEGER NOT NULL, CONSTRAINT fk_category FOREIGN KEY (exercise_id) REFERENCES lifts(id), CONSTRAINT fk_category FOREIGN KEY (workout_id) REFERENCES workouts(id));
-- CREATE TABLE workouts (id INTEGER AUTO_INCREMENT PRIMARY KEY, name varchar(200), date_time DATETIME NOT NULL DEFAULT NOW(), user_id INTEGER NOT NULL, CONSTRAINT fk_category FOREIGN KEY (user_id) REFERENCES users(id));
-- CREATE TABLE lifts (id INTEGER AUTO_INCREMENT PRIMARY KEY, category VARCHAR(200) NOT NULL, name varchar(200) NOT NULL);